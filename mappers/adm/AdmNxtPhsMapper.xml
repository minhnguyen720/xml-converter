<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dou.adm.mappers.AdmNxtPhsMapper">

    <insert id="insert" parameterType="AdmNxtPhsVO">
		insert  /** adm.AdmNxtPhs.insert */
		into ADM_NXT_PHS(
				NXT_PHS_ID,
				PHS_CD,
				PROC_PHS_ID,
				cre_usr_id, cre_dt, upd_usr_id, upd_dt
				)
		values(
				#{nxtPhsId},
				#{phsCd},
				#{procPhsId},
				#{creUsrId}, sysdate, #{updUsrId}, sysdate
				)
	</insert>
</mapper>